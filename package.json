{
  "name": "imvu-party-guide",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "vite preview --host 0.0.0.0 --port ${PORT:-4173}"
  },
  "devDependencies": {
    "vite": "^5.1.4"
  }
}
